Place the macOS dcraw_emu binary here:
  bin/macos/dcraw_emu

Build it from the LibRaw source or install via Homebrew:
  brew install libraw
  cp $(which dcraw_emu) bin/macos/dcraw_emu

Or download from official site:
  LibRaw: https://www.libraw.org/download
